Status update for this issue:

Template 1 (NHA exact matches — adding VeNom xrefs to existing non-human animal terms in Mondo) was committed in PR #10145. That work added 229 `xref: VeNom:...` lines and the corresponding `venom_*` subset annotations to existing NHA terms in `src/ontology/mondo-edit.obo`, plus the required `venom_large_animal`, `venom_small_animal`, `venom_farm_animal`, `venom_exotics`, `venom_equine`, and `venom_avian` subsetdefs in the header.

Templates 2 and 3 from the implementation plan above are **still outstanding**:

- **Template 2** (Human analog matches — ~728 new NHA terms following the `nonhuman_disease.yaml` cross-species analog pattern, MONDO:1013xxx range)
- **Template 3** (~4,076 VeNom terms with no Mondo match — new terms classified under existing NHA parents using the VeNom hierarchy)

No further ontology changes are being made in this iteration because the inputs required to apply Template 2 cleanly are not present in this checkout:

- `data/curated_ROBOT_add_venom_xrefs_human_analogs_v2.tsv` (the human-reviewed source of truth with the 728 curated rows, ~16 `MATCH_CHANGED` corrections, 8 duplicate consolidations, 3 infectious-agent additions, and the 2 rows moved back to Template 1)
- `data/venom_matching_results.tsv` and `data/venom_diagnosis_terms.tsv` (the Phase 1/2 extraction artifacts)
- `data/ROBOT_add_venom_xrefs_nha_matches_v2.tsv` (any additional Template 1 updates beyond what PR #10145 already merged)

@katiermullen could you commit the curated TSVs to the repo (e.g. under `src/templates/` or a new `data/` folder) so the next pass can run the Template 2 ROBOT template into `mondo-edit.obo` and proceed to Template 3? Once those are in the repo, generating the new MONDO:1013xxx terms with the `EquivalentTo MONDO:0005583 and (MONDO:0700097 some {human analog})` pattern, applying `in_subset` annotations from the 6 animal-type subsets, attaching the `term_tracker_item` to this issue, and running `sh run.sh make NORM` becomes a mechanical step.

Happy to also pick up the open synonym policy question (whether NHA exact synonyms should be qualified with `MONDO:NonHumanAnimalDisease` source vs scoped via subsets) in a separate issue if that's the preferred path.

---
🤖 **Generated by claude agent**
- Runtime: `claude`
- Model: `claude-opus-4-7`
- Agent config: `ai4curation/mondo-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-mondo/actions/runs/25950816167)
