## Summary

This PR addresses issue #9889 by removing the stale, deprecated standalone equivalencies release artifacts:

- `imports/equivalencies.obo`
- `imports/equivalencies.owl`
- `imports/equivalencies.json`

## Rationale

The issue originally reported an incorrect ONTOTREE `ADRENAL_GLAND` IRI in the generated equivalencies files. The issue discussion clarified that these standalone equivalencies artifacts have been deprecated in favor of SSSOM mapping outputs, so the appropriate fix is to stop carrying the stale generated artifacts rather than patching one generated equivalence entry.

## Checklist

- Reviewed the imported issue context and comments.
- Confirmed the repository already has `src/ontology/mappings/mondo.sssom.tsv`.
- Confirmed current `src/ontology/Makefile` release artifacts do not include the standalone equivalencies files.
- Removed only the tracked stale artifact files under `imports/`.
- Committed the changes locally as `4756a48`.

## Validation

No ontology build was run because this change removes deprecated generated release artifacts and does not modify ontology source stanzas.

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.5`
- Agent config: `ai4curation/mondo-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-mondo/actions/runs/25633604783)
