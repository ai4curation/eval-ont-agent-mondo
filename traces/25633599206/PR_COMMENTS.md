## Summary

Updated `src/patterns/dosdp-patterns/inborn_metabolic_disrupts.yaml` to correct the definition template for inborn errors of metabolism caused by disruption of a process.

## Rationale

The pattern's logical definition already uses `inborn errors of metabolism` (`MONDO:0019052`), but the generated textual definition incorrectly described the disease as acquired and contained the grammar typo `is has`. The template now reads: `An inherited metabolic disease that has its basis in the disruption of %s.`

## Checklist

- Confirmed the issue context and requested file.
- Made the minimal change to the DOSDP pattern definition text.
- Verified the YAML parses successfully with `python -c 'import yaml; ...'`.
- Committed only the edited pattern file.

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.5`
- Agent config: `ai4curation/mondo-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-mondo/actions/runs/25633599206)
