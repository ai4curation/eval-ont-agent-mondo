Updated `MONDO:1060138` (`GRIN-related complex neurodevelopmental disorder`) by adding literature-backed synonyms and a tracker link to issue #9930.

Changes made:
- Added `GRIN-related neurodevelopmental disorder` as an `EXACT` synonym, supported by `PMID:36619673`.
- Added `GRIN-related encephalopathy` as a `RELATED` synonym, supported by `doi:10.1093/brain/awae041`.
- Added `grinpathies` as a `RELATED` synonym, supported by `PMID:34884460`.
- Added `IAO:0000233 "https://github.com/monarch-initiative/mondo/issues/9930"` to link the term back to this request.

Rationale:
- The requested umbrella labels are supported in the recent GRIN literature, but I did not add `GRIN-related complex neurodevelopmental disorder` as a synonym because it is already the primary label.
- I used `grinpathies` instead of `GRINopathies` because the verifiable published usage I found uses `grinpathies`/`the so-called grinpathies` rather than the capitalized spelling.
- I kept `GRIN-related encephalopathy` as `RELATED` rather than `EXACT`, because it is used in the literature as an umbrella label but is slightly less precise than the MONDO preferred name.

Checks completed:
- Reviewed the imported issue context in `__issue_context__.json`.
- Verified current MONDO content for `MONDO:1060138` and nearby GRIN-related terms.
- Verified synonym usage against the cited literature / publisher pages.
- Ran ODK normalization with `make NORM` and replaced `src/ontology/mondo-edit.obo` with the normalized output.
- Ran `robot convert --catalog src/ontology/catalog-v001.xml -i src/ontology/mondo-edit.obo -f obo -o mondo-edit.TMP.obo` successfully as a syntax check.

Local commit:
- `f8c259c` `Add GRIN-related disorder synonyms`

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.4`
- Agent config: `ai4curation/mondo-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-mondo/actions/runs/25949208290)
