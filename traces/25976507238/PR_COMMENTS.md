Implemented the obsoletions requested in issue `#9795` for four MONDO terms that correspond to OMIM entries merged into other concepts.

Changes made:

- Converted `MONDO:0009027` to an obsolete term with `replaced_by: MONDO:0007402`
- Converted `MONDO:0011961` to an obsolete term with `replaced_by: MONDO:0044720`
- Converted `MONDO:0010553` to an obsolete term with `replaced_by: MONDO:0010549`
- Converted `MONDO:0013935` to an obsolete term with `replaced_by: MONDO:0012273`
- Added `property_value: IAO:0000231 MONDO:TermsMerged` to each obsolete stanza
- Kept the issue tracker link on each obsolete stanza as `IAO:0000233`
- Removed the prior obsoletion-candidate content so each obsolete stanza now contains only the expected minimal obsoletion metadata

Rationale:

- The issue states these source OMIM concepts have been merged into the listed target OMIM concepts.
- MONDO policy in this repository is to represent this as obsoletion with direct replacement rather than keeping the old term active.
- The resulting obsolete stanzas are consistent with the project guidance for merged/obsoleted terms.

Checks completed:

- Read the imported issue context from `__issue_context__.json`
- Inspected all four source terms and the proposed replacement terms in `src/ontology/mondo-edit.obo`
- Verified the four source MONDO IDs no longer have lingering references outside their own obsolete stanzas after the edit
- Reviewed the final `git diff` to confirm the change was limited to `src/ontology/mondo-edit.obo`

Validation notes:

- I attempted to run the required normalization/syntax validation workflow, but this environment does not have `docker`, so ODK-backed commands such as `make NORM` and `robot convert` could not be executed locally here.
- I also attempted to use `aurelian` for PMID lookups mentioned on one source term, but `aurelian` is not installed in this environment. Those PMIDs were removed from the obsolete stanza as part of the obsoletion, and no new external identifiers were introduced.

Commit:

- `75dd4a8` `Obsolete OMIM merged terms`

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.4`
- Agent config: `ai4curation/mondo-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-mondo/actions/runs/25976507238)
