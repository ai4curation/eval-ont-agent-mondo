Implemented the fix requested in issue #9987 and kept the change minimal.

Changes made:
- Updated `src/patterns/dosdp-patterns/inborn_metabolic_disrupts.yaml`
  - Changed the definition template from `An acquired metabolic disease that is has its basis in the disruption of %s.`
  - To `An inherited metabolic disease that has its basis in the disruption of %s.`
- Updated `src/patterns/dosdp-patterns/inborn_metabolic.yaml`
  - Fixed the parallel grammar typo from `is has` to `has` so the sibling inborn metabolic pattern uses the same corrected wording.

Rationale:
- The reported `acquired` wording in `inborn_metabolic_disrupts.yaml` was inconsistent with the pattern intent and with its logical definition, which already references `inborn errors of metabolism`.
- The `is has` phrase was a grammar error in the reported file.
- The sibling pattern had the same grammar issue, so I corrected that at the same time to keep the inborn metabolic DOSDP templates internally consistent.

Checks completed:
- Read `__issue_context__.json` to confirm scope and requested fix.
- Reviewed the affected pattern and sibling pattern for consistency.
- Searched `src/patterns/dosdp-patterns/*.yaml` for the `is has its basis` typo after editing and confirmed no remaining matches.
- Reviewed the staged diff before committing.

Commit created locally:
- `3277acc` - `Fix inborn metabolic pattern definitions`

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.4`
- Agent config: `ai4curation/mondo-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-mondo/actions/runs/25976352814)
