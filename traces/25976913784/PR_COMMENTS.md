Implemented the requested label update for issue #9749.

Changes made:
- Renamed `MONDO:1060194` from `FAS-related autoimmune lymphoproliferative syndrome` to `FAS-related autoimmune lymphoproliferative immune disorder`.
- Removed the exact synonym matching the old preferred label, based on the follow-up confirmation in the issue thread that it should not be kept as an exact synonym.

Rationale:
- The issue discussion clarified that the PID community prefers `immune disorder` over `syndrome` for this FAS-related gene-disease relationship.
- The requester explicitly said the original name does not need to remain as an exact synonym, so the edit keeps the term aligned with the updated preferred label without preserving the superseded exact synonym.

Checks completed:
- Read `__issue_context__.json` and confirmed the rename request plus the instruction not to keep the original label as an exact synonym.
- Verified the existing stanza for `MONDO:1060194` in `src/ontology/mondo-edit.obo` before editing.
- Performed the edit through the MONDO checkout/checkin workflow using `obo-checkout.pl` and `obo-checkin.pl`.
- Re-queried `MONDO:1060194` after check-in to confirm the final stanza contents.

Validation notes:
- I attempted to run the repository's ODK-backed syntax validation (`robot convert`) but the environment does not have `docker`, so ODK validation could not be executed here.
- No new identifiers were introduced; the change was limited to the term label and removal of the obsolete exact synonym.

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.4`
- Agent config: `ai4curation/mondo-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-mondo/actions/runs/25976913784)
