## Summary

Updated `src/ontology/mondo-edit.obo` with the latest externally managed content, including the rare disease subset refresh requested in issue/PR #9898.

## What changed

- Ran the externally managed content update pipeline.
- Ran the rare disease subset update pipeline.
- Normalized `mondo-edit.obo` after the generated updates.
- The resulting ontology diff updates externally managed annotations/mappings such as curated content resources and externally managed synonym provenance, along with rare subset-managed content.

## Validation

Completed:

- [x] `cd src/ontology && make update-external-content && make update-rare-disease-subset && make NORM && mv NORM mondo-edit.obo`
- [x] `cd src/ontology && robot convert --catalog catalog-v001.xml -i mondo-edit.obo -f obo -o mondo-edit.TMP.obo`
- [x] Removed temporary validation output `mondo-edit.TMP.obo`
- [x] `git diff --check -- src/ontology/mondo-edit.obo`

Notes:

- Docker was not available in this environment, so the repository-local command-line tools (`robot`, `owltools`, `make`) were used directly.
- Subset metrics reported by the update pipeline were unchanged before/after the rare disease refresh in this run.

---
🤖 **Generated by pi agent**
- Runtime: `pi`
- Model: `openai/gpt-5.5`
- Agent config: `ai4curation/mondo-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-mondo/actions/runs/25633633894)
