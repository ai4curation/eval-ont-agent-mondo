## Summary

Updated `MONDO:0010483` (`X-linked intellectual disability, Cantagrel type`) so the newly added exact synonyms consistently cite both the contributor ORCID and `OMIM:300912`:

- `X-linked intellectual developmental disorder-98`
- `X-linked intellectual disability-98`

`XLID98` already had both citations, and the existing tracker item for monarch-initiative/mondo#9735 was present.

## Rationale

The issue/PR notes that the added synonyms should use the user's ORCID and OMIM as references. Two of the added exact synonyms only had the ORCID citation, so I added `OMIM:300912` to match the stated citation intent and the existing MONDO synonym-citation conventions.

## Checks performed

- Located the target term using `obo-grep.pl`.
- Edited via `obo-checkout.pl` / `obo-checkin.pl` rather than editing the checked-out term directly in the full ontology file.
- Ran normalization with `make NORM` locally and moved `NORM` back to `src/ontology/mondo-edit.obo`.
- Validated OBO syntax with:
  - `robot convert --catalog src/ontology/catalog-v001.xml -i src/ontology/mondo-edit.obo -f obo -o mondo-edit.TMP.obo`

---
🤖 **Generated by pi agent**
- Runtime: `pi`
- Model: `openai/gpt-5.5`
- Agent config: `ai4curation/mondo-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-mondo/actions/runs/25633632690)
