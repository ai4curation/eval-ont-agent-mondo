## Status update on VeNom integration

### Current state of work on this issue

**Template 1 (NHA exact matches — add VeNom xrefs to existing Mondo NHA terms): complete.**

The previous PR (#10145) merged Template 1 into `src/ontology/mondo-edit.obo`. The current state of the edit file contains:
- 229 `xref: VeNom:NNNNN {source="MONDO:equivalentTo"}` statements across 209 distinct Mondo NHA terms
- 704 `subset: venom_*` annotations (animal-type subsets from the VeNom Terms sheet)
- 6 `subsetdef:` declarations in the header (`venom_avian`, `venom_equine`, `venom_exotics`, `venom_farm_animal`, `venom_large_animal`, `venom_small_animal`)
- `term_tracker_item` links to this issue on the updated terms

This matches the v3 plan in your 2026-04-10 comment (231 row template; some VeNom IDs share a Mondo target, so 229 xrefs ≈ ~209 distinct terms).

### Next steps blocked: need the curated TSVs

Templates 2 and 3 described in your 2026-04-14 comment are **not yet applied** to `mondo-edit.obo`, and I am not able to proceed in this round because the inputs are not available in this repository:

- `data/curated_ROBOT_add_venom_xrefs_human_analogs_v2.tsv` (728 rows, Template 2 — new "{disease}, non-human animal" terms with MONDO:1013000–MONDO:1013793, cross-species analog pattern)
- `data/curated_ROBOT_add_venom_xrefs_nha_matches_v2.tsv` (Template 1 source — already applied)
- Template 3 source for the ~4,076 no-match VeNom terms
- The upstream VeNom release h (Feb 2026) extracts (`data/venom_diagnosis_terms.tsv`, `data/venom_matching_results.tsv`)

Per the project's "never guess identifiers" rule, I will not synthesize VeNom codes, gene IDs, taxon IDs, or Mondo IDs from memory or fabricate the new NHA term IDs without the reviewed source.

### Suggested unblock

@katiermullen — could you push the curated `data/` directory (or attach the TSVs to this issue) so the next iteration can run the ROBOT templates and re-normalize the edit file? Once the curated TSV is in the repo, the steps for Template 2 are straightforward (template merge → `sh run.sh make NORM`).

No changes committed in this round.

---
🤖 **Generated by claude agent**
- Runtime: `claude`
- Model: `claude-opus-4-7`
- Agent config: `ai4curation/mondo-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-mondo/actions/runs/25963757918)
