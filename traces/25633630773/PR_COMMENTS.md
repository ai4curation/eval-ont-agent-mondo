## Summary

Addresses #9889 by fixing the malformed ADRENAL_GLAND identifier in the deprecated equivalencies OBO artifact.

Changes made:
- Updated `imports/equivalencies.obo` so the external equivalency for `MONDO:0021227` is:
  - `http://oncotree.mskcc.org/api/tumorTypes/search/code/ADRENAL_GLAND`
  - instead of the incorrect `ADRENAL:GLAND` OBO-style CURIE generated by conversion.
- Updated `src/ontology/Makefile` so the `imports/equivalencies.obo` target applies the same post-conversion correction after `ROBOT convert`, preventing regeneration from reintroducing `ADRENAL:GLAND`.

## Rationale

The OWL and JSON equivalencies files already contained the correct ONCOTREE IRI. The OBO conversion was contracting the ONCOTREE code with an underscore into the invalid-looking prefix/local form `ADRENAL:GLAND`. This change keeps the OBO artifact aligned with the OWL/JSON outputs and with the expected ONCOTREE IRI.

## Checks performed

- Confirmed `imports/equivalencies.owl` and `imports/equivalencies.json` already contain `http://oncotree.mskcc.org/api/tumorTypes/search/code/ADRENAL_GLAND`.
- Confirmed `imports/equivalencies.obo` previously contained `id: ADRENAL:GLAND` for the `MONDO:0021227` equivalency.
- Verified after the edit that `imports/equivalencies.obo` contains the expected ONCOTREE IRI and no longer contains `id: ADRENAL:GLAND`.
- Ran `git diff --check` on the edited files.

---
🤖 **Generated by pi agent**
- Runtime: `pi`
- Model: `openai/gpt-5.5`
- Agent config: `ai4curation/mondo-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-mondo/actions/runs/25633630773)
