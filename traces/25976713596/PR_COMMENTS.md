Updated `MONDO:0023124` according to the resolution in issue `#9799` discussion: the term is retained and relabeled as `Dursun syndrome` rather than obsoleted.

Changes made:

- Renamed `MONDO:0023124` from `familial pulmonary arterial hypertension leucopenia and atrial septal defect` to `Dursun syndrome`.
- Replaced the old GARD-derived synonym set with a synonym treatment aligned to the discussed OMIM included entry:
  - `"familial pulmonary arterial hypertension leucopenia and atrial septal defect" EXACT [OMIM:612541]`
  - `"familial pulmonary arterial hypertension, leucopenia and ASD" RELATED [OMIM:612541]`
- Added `xref: OMIM:612541 {source="MONDO:includedEntryInOMIM"}`.
- Added `xref: Orphanet:178503 {source="MONDO:equivalentObsolete"}`.
- Removed the previous obsoletion-candidate comment/subset and the invalid GARD `seeAlso` link that motivated the original obsoletion request.

Rationale:

- The issue comments superseded the original obsoletion request and converged on keeping `MONDO:0023124` as `Dursun syndrome`.
- `OMIM:612541` was explicitly discussed as an OMIM included entry rather than a standard equivalent mapping.
- `Orphanet:178503` was explicitly discussed as an obsolete Orphanet concept, so it was modeled with `MONDO:equivalentObsolete`.
- The previous GARD reference was non-functional and was the only support for the original label, so it was removed.

Checks completed:

- Read the imported issue context and followed the issue-thread resolution rather than the original title/body alone.
- Verified existing MONDO usage patterns for `MONDO:includedEntryInOMIM` and `MONDO:equivalentObsolete` before adding new xrefs.
- Confirmed `OMIM:612541` is already used elsewhere in MONDO in the related G6PC3-associated term context.
- Ran `robot convert --catalog src/ontology/catalog-v001.xml -i src/ontology/mondo-edit.obo -f obo -o /tmp/opencode/mondo-edit.TMP.obo` successfully as a syntax check.

Validation note:

- I could not run the repo’s requested `make NORM` Docker-based normalization step because `docker` is not available in this environment. The ontology edit was still syntax-checked successfully with local `robot convert`.

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.4`
- Agent config: `ai4curation/mondo-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-mondo/actions/runs/25976713596)
