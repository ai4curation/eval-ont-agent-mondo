## Summary

Fixed the definition template for `inborn_metabolic_disrupts.yaml` so it now describes an inherited metabolic disease rather than an acquired metabolic disease, and corrected the grammar from "is has" to "has".

I also corrected the same "is has" grammar typo in the sibling `inborn_metabolic.yaml` template and updated the tracked pattern OWL artifacts so their definition annotations match the YAML sources.

## Rationale

The `inborn_metabolic_disrupts.yaml` pattern is logically defined under inborn errors of metabolism, so the text definition should say "inherited metabolic disease". Keeping the generated pattern OWL files in sync avoids leaving stale generated annotations with the old wording.

## Checklist

- Reviewed issue #9987 context.
- Confirmed the bad definition text in `src/patterns/dosdp-patterns/inborn_metabolic_disrupts.yaml`.
- Checked related inborn metabolic pattern wording.
- Updated the source YAML templates.
- Updated tracked generated pattern OWL annotations with the same definition text.
- Verified no remaining copies of the bad acquired/grammar strings under `src/patterns`.
- Validated edited YAML files parse successfully.
- Validated edited RDF/XML pattern files parse successfully.
- Ran `git diff --check` for the edited files.

## Validation

- `python -c 'import yaml,sys; [yaml.safe_load(open(f)) for f in sys.argv[1:]]; print("OK YAML")' src/patterns/dosdp-patterns/inborn_metabolic.yaml src/patterns/dosdp-patterns/inborn_metabolic_disrupts.yaml`
- `python -c 'import xml.etree.ElementTree as ET,sys; [ET.parse(f) for f in sys.argv[1:]]; print("OK XML")' src/patterns/pattern-simple.owl src/patterns/pattern.owl src/patterns/pattern-merged.owl src/patterns/pattern-with-imports.owl`
- `git diff --check -- src/patterns/dosdp-patterns/inborn_metabolic_disrupts.yaml src/patterns/dosdp-patterns/inborn_metabolic.yaml src/patterns/dosdp-pattern.owl src/patterns/pattern-simple.owl src/patterns/pattern.owl src/patterns/pattern-merged.owl src/patterns/pattern-with-imports.owl`

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.5`
- Agent config: `ai4curation/mondo-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-mondo/actions/runs/25633572954)
