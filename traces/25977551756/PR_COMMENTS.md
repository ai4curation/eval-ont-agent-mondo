Implemented issue #9493 by updating `MONDO:0005709` (`common cold`) in `src/ontology/mondo-edit.obo`.

Changes made:
- Added `is_a: MONDO:0005550 ! infectious disease` to classify common cold under infectious disease.
- Added provenance to that new parent axiom with:
  - `source="https://github.com/monarch-initiative/mondo/issues/9493"`
  - `source="https://orcid.org/0000-0003-2955-4640"`
- Updated `property_value: IAO:0000233` from the previous issue link to `https://github.com/monarch-initiative/mondo/issues/9493` so the edited term points back to the current curation request.

Rationale:
- The issue requested adding `infectious disease` as a parent for `common cold`.
- The thread explicitly asked to implement the asserted parent addition without adding a logical definition.
- The existing text definition already states that common cold is usually caused by viruses, so the added asserted parent is consistent with the term content.

Checks completed:
- [x] Read imported issue context and curator instructions.
- [x] Inspected the existing `MONDO:0005709` and `MONDO:0005550` stanzas.
- [x] Applied a minimal asserted-parent edit only.
- [x] Verified ontology syntax locally with:
  - `robot convert --catalog src/ontology/catalog-v001.xml -i src/ontology/mondo-edit.obo -f obo -o /tmp/opencode/mondo-edit.TMP.obo`
- [ ] Ran ODK normalization (`make NORM`) and Docker-based validation.

Validation note:
- `robot convert` succeeded with no errors.
- I could not run `sh run.sh make NORM` or the equivalent Docker-based ODK workflow in this environment because `docker` is not installed (`docker: command not found`).

Commit:
- `5e08065` `Add infectious disease parent to common cold`

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.4`
- Agent config: `ai4curation/mondo-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-mondo/actions/runs/25977551756)
