This change addresses issue #9871 for `MONDO:0009106` (`diastematomyelia`).

Summary of ontology edits:
- Replaced the exact Orphanet mapping from `Orphanet:1671` (`split cord malformation type I`) with `Orphanet:573278` (`split cord malformation`).
- Updated the Orphanet-derived subsets to match the broader Orphanet group concept and added `subset: disease_grouping` to reflect that MONDO is representing the broader SCM concept rather than the type I subtype.
- Changed `SCM type 1` and `split cord malformation type 1` from `EXACT` to `NARROW` synonyms so the term no longer asserts exact equivalence to the narrower subtype.
- Promoted `split cord malformation` and `split spinal cord malformation` to `EXACT` synonyms, supported by the existing GARD/MEDGEN context and the requested Orphanet reassignment.
- Added an `IAO:0000233` tracker link back to issue `#9871`.

Rationale:
- The issue correctly identified that `Orphanet:1671` is too narrow for the current MONDO concept.
- The existing MONDO term already behaved as a broader split-cord-malformation concept in its non-Orphanet content, so the smallest consistent fix was to realign the Orphanet mapping and synonym scopes rather than create or restore another MONDO term.
- Keeping the type I labels as `NARROW` preserves useful terminology without over-asserting equivalence.

Checks completed:
- Reviewed the issue context from `__issue_context__.json`.
- Inspected the active MONDO stanza and existing references to `Orphanet:1671` and `Orphanet:573278`.
- Checked local MONDO usage patterns for active `ordo_group_of_disorders` mappings before assigning the new Orphanet subset/xref.
- Normalized the ontology serialization with ODK (`make NORM`, then replaced `mondo-edit.obo` with the normalized output).
- Ran `robot convert --catalog catalog-v001.xml -i mondo-edit.obo -f obo -o mondo-edit.TMP.obo` successfully as a syntax check.

Local commit:
- `8cc524d` `Fix MONDO:0009106 Orphanet mapping`

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.4`
- Agent config: `ai4curation/mondo-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-mondo/actions/runs/25949215720)
