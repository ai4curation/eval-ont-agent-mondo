This change updates `MONDO:0011996` to use `chronic myeloid leukemia` as the primary label, matching the preferred contemporary terminology requested in issue `#9892`. The older label `chronic myelogenous leukemia` was retained as an exact synonym, and the more explicit form `chronic myeloid leukemia, BCR-ABL1 positive` was added as an exact synonym so the fusion-defined wording remains available for search and mapping contexts.

I also added a `term_tracker_item` link back to issue `#9892`.

Completed checklist:
- Located the target term in `src/ontology/mondo-edit.obo` and confirmed the requested preferred wording was already present as an exact synonym.
- Checked out `MONDO:0011996` into `terms/`, edited the term there, and checked it back in.
- Ran ODK normalization with `make NORM` and replaced `src/ontology/mondo-edit.obo` with the normalized output.
- Ran `robot convert --catalog catalog-v001.xml -i mondo-edit.obo -f obo -o mondo-edit.TMP.obo` to confirm the normalized ontology converts cleanly.
- Removed transient normalization / validation artifacts before committing.

Validation notes:
- The final diff includes the expected label update for `MONDO:0011996`, synonym adjustment, issue tracker annotation, and comment-only parent label updates on terms that reference `MONDO:0011996`.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.4`
- Agent config: `ai4curation/mondo-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-mondo/actions/runs/25949213448)
