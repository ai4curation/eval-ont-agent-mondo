No changes were committed in this iteration.

## Summary

Issue #5726 is a multi-phase VeNom integration. Phase 1 / Template 1 (NHA exact matches — adding `VeNom:` xrefs and `venom_*` subset annotations to existing non-human animal terms) was already completed and merged via PR #10145 (commit `7fe96d4`). The current `src/ontology/mondo-edit.obo` already contains:

- 6 `subsetdef:` lines for `venom_avian`, `venom_equine`, `venom_exotics`, `venom_farm_animal`, `venom_large_animal`, `venom_small_animal`
- 933 `VeNom:` xrefs with `{source="MONDO:equivalentTo"}`
- Animal-type `subset:` annotations sourced to the relevant VeNom IDs

## Why no further changes were made

The remaining work described in the issue is:

- Template 2: 728 new NHA terms (`MONDO:1013000`–`MONDO:1013793`) via the `nonhuman_disease.yaml` cross-species analog pattern
- Template 3: ~4,076 additional new VeNom terms with no current Mondo match

Both depend on curated input files that are not in this working copy:

- `data/curated_ROBOT_add_venom_xrefs_human_analogs_v2.tsv` (Template 2 source of truth)
- The VeNom release h TermSubset / Relationship sheets
- The downstream review of the Google Sheet referenced in the prior issue comment

Without these inputs, generating ~700+ new ontology terms with correct parents, cross-species analog logical definitions, gene/infectious-agent identifiers, and animal-type subsets would amount to fabrication. Per the MONDO curation guidelines in `CLAUDE.md` ("never guess identifiers of any kind", "if you include some identifier that was not provided by the user, you MUST check it"), I declined to produce speculative terms.

## Checks performed

- Confirmed Template 1 already merged: `git log` shows `7fe96d4 add venom xrefs (#10145)` on the current branch lineage.
- Confirmed `VeNom:` xref count in `mondo-edit.obo` (933) and presence of the six `venom_*` subset definitions.
- Confirmed the `MONDO:1013xxxx` ID range planned for Template 2 is unused (highest existing ID in `MONDO:101xxxx` is `MONDO:1012987`).
- Confirmed `data/` and `scratch/` do not contain the curated VeNom TSVs referenced in the implementation plan.

## Next steps (suggested for follow-up PRs)

1. Check `data/curated_ROBOT_add_venom_xrefs_human_analogs_v2.tsv` into the repo (or export from the linked Google Sheet) so Template 2 can be applied via a ROBOT template run.
2. Open a separate PR for Template 2 once the input file is available; it should:
   - Use IDs `MONDO:1013000`–`MONDO:1013793`
   - Apply the `nonhuman_disease.yaml` DOSDP pattern (definition: "{human disease} that occurs in non-human animals.")
   - Add `EquivalentTo MONDO:0005583 and (MONDO:0700097 some {human analog})`
   - Add `VeNom:` xrefs with `{source="MONDO:equivalentTo"}`, animal-type subsets, and `term_tracker_item` to issue #5726
   - Re-normalise with `sh run.sh make NORM && mv NORM mondo-edit.obo`
3. Open a third PR for Template 3 after the corresponding curated TSV is finalised.

---
🤖 **Generated by claude agent**
- Runtime: `claude`
- Model: `claude-opus-4-7`
- Agent config: `ai4curation/mondo-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-mondo/actions/runs/25974602403)
