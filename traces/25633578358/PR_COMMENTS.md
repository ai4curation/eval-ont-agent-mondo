## Summary

Addressed issue #9889 by correcting the OncoTree `ADRENAL_GLAND` equivalency serialization and aligning the SSSOM OncoTree prefix map with the real OncoTree URL namespace.

## Changes

- Updated `imports/equivalencies.obo` so the affected class ID is `http://oncotree.mskcc.org/api/tumorTypes/search/code/ADRENAL_GLAND` instead of the incorrect `ADRENAL:GLAND`.
- Updated the `imports/equivalencies.obo` generation rule to apply the same correction if the deprecated equivalencies OBO artifact is regenerated.
- Updated `src/ontology/metadata/mondo.sssom.config.yml` and the generated SSSOM TSV headers so `ONCOTREE:` expands to `http://oncotree.mskcc.org/api/tumorTypes/search/code/`.

## Rationale

The OWL equivalencies artifact already used the correct OncoTree IRI, but the OBO serialization collapsed `ADRENAL_GLAND` into the invalid pseudo-CURIE `ADRENAL:GLAND`. Since the issue discussion notes that equivalencies artifacts are deprecated in favor of SSSOM, the SSSOM prefix map was also corrected so OncoTree CURIEs resolve to the same intended URL namespace.

## Checklist

- Read `__issue_context__.json`.
- Checked the MONDO stanza for `MONDO:0021227` using `obo-grep.pl`.
- Verified `imports/equivalencies.owl` already contained the correct OncoTree URL.
- Verified `imports/equivalencies.obo` no longer has the stray `ADRENAL` ID prefix.
- Ran `git diff --check`.
- Ran `robot convert --catalog src/ontology/catalog-v001.xml -i imports/equivalencies.obo -f owl -o /tmp/equivalencies-check.owl`.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.5`
- Agent config: `ai4curation/mondo-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-mondo/actions/runs/25633578358)
