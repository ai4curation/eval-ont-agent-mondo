Updated the Orphanet mapping for isolated megalencephaly in `src/ontology/mondo-edit.obo`.

Summary of changes:
- Removed the live `xref: Orphanet:2477 {source="MONDO:equivalentTo"}` from `MONDO:0016608` (`megalencephaly`).
- Removed the `Orphanet:2477`-derived Orphanet subset tags from `MONDO:0016608`.
- Added the live `Orphanet:2477` equivalent mapping to `MONDO:0017089` (`isolated megalencephaly`).
- Added the corresponding Orphanet subset tags to `MONDO:0017089`.
- Updated the Orphanet-backed parent provenance on `MONDO:0017089` from obsolete `Orphanet:268920` to current `Orphanet:2477`.
- Added `IAO:0000233` issue-tracker links for issue `#9854` on the two touched terms.

Rationale:
- The issue requested moving `ORPHANET 2477` from the broad `megalencephaly` term to `isolated megalencephaly`.
- I verified against the current Orphanet record that `ORPHA:2477` is the active concept for isolated megalencephaly, so the live Orphanet mapping belongs on `MONDO:0017089`, not `MONDO:0016608`.
- `MONDO:0017089` already carried the matching disease granularity and retained the obsolete `Orphanet:268920` xref, so this was a focused remap rather than a term restructuring.

Checks completed:
- Verified the issue context in `__issue_context__.json`.
- Confirmed the affected MONDO stanzas with `obo-grep.pl`.
- Checked out and edited the affected terms via `obo-checkout.pl` / `obo-checkin.pl`.
- Ran normalization with `cd src/ontology && sh run.sh make NORM && mv NORM mondo-edit.obo`.
- Ran syntax validation with `cd src/ontology && sh run.sh robot convert --catalog catalog-v001.xml -i mondo-edit.obo -f obo -o mondo-edit.TMP.obo`.

Commit:
- `d670dff` `Fix Orphanet mapping for isolated megalencephaly`

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.4`
- Agent config: `ai4curation/mondo-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-mondo/actions/runs/25949229706)
