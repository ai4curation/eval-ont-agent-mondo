## Summary

Updated externally managed MONDO content in `src/ontology/mondo-edit.obo`.

Changes include:
- Added current MalaCards `curated_content_resource` annotations.
- Added explicit `MONDO:CLINGEN` and `MONDO:NORD` synonym provenance to externally managed preferred-label synonyms.
- Updated `src/ontology/reports/source-versions.tsv` from `mondo-ingest` source versions.

## Rationale

Issue #9898 requested an externally managed content update, including rare disease subsets. I ran the repository EMC and rare-subset update pipeline and then also ran the MalaCards and EMC synonym provenance steps from the standalone EMC target, because the combined `update-external-content-incl-rare` target does not include those two steps.

The rare disease subset metrics were unchanged before vs. after the combined update target.

## Completed Checklist

- Read `__issue_context__.json`.
- Ran `make update-external-content-incl-rare`.
- Ran `make update-malacards update-emc-synonym-prov`.
- Refreshed `reports/source-versions.tsv` from `mondo-ingest`.
- Ran final `make NORM && mv NORM mondo-edit.obo`.
- Validated OBO syntax with `robot convert --catalog catalog-v001.xml -i mondo-edit.obo -f obo -o mondo-edit.TMP.obo`.
- Ran `git diff --check`.
- Committed only the changed tracked files.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.5`
- Agent config: `ai4curation/mondo-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-mondo/actions/runs/25633581790)
