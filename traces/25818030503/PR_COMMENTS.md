No ontology changes were committed in this iteration.

## Why

Issue #5726 is being implemented in three sequential ROBOT-template phases per the curator's plan:

1. **Template 1 — NHA exact matches (231 xrefs).** Already merged in PR #10145. Confirmed by inspection: `src/ontology/mondo-edit.obo` contains 229 `xref: VeNom:NNNN` lines and the six `venom_*` subset declarations (`venom_avian`, `venom_equine`, `venom_exotics`, `venom_farm_animal`, `venom_large_animal`, `venom_small_animal`) in the header.
2. **Template 2 — 728 new NHA terms (MONDO:1013000–MONDO:1013793) via the `nonhuman_disease.yaml` cross-species analog pattern.** Marked "reviewed" by the curator. This is the natural next phase.
3. **Template 3 — 4,076 unmatched terms.** Not started.

To execute Template 2 I need the curated TSV (`data/curated_ROBOT_add_venom_xrefs_human_analogs_v2.tsv`) that the curator's comment refers to. It is not present in this repository — neither in `data/`, `src/templates/`, nor anywhere reachable by `find`/`grep`. Without it, generating 728 new MONDO terms with correct logical definitions (each tied to a specific human analog), VeNom xrefs, animal-type subset memberships, and infectious-agent NCBITaxon links would require inventing the mappings, which would be unsafe given the explicit project rule that we never guess identifiers.

## Checklist completed

- [x] Read `__issue_context__.json` and the curator's three implementation plan comments
- [x] Inspected current branch state: previous PR #10145 ("add venom xrefs") is the only commit beyond main
- [x] Verified Template 1 output present in `mondo-edit.obo` (229 VeNom xrefs, 6 subset declarations)
- [x] Checked for Template 2/3 source data: no `venom_*` TSVs, no `curated_*` TSVs, no `ROBOT_add_venom_*` files anywhere in repo
- [x] Confirmed MONDO:1013xxx ID range is unused (highest existing MONDO:101xxxx is MONDO:1012987), so Template 2's planned IDs are still safe to claim
- [x] Asked the curator for the missing data file in `ISSUE_COMMENTS.md`

## Next steps once data is provided

1. Apply the curated Template 2 TSV to produce 728 new term stanzas following the `nonhuman_disease.yaml` pattern:
   - Name: "{human disease}, non-human animal"
   - Definition: "{human disease} that occurs in non-human animals." with xref `MONDO:patterns/nonhuman_disease`
   - Logical def: `EquivalentTo MONDO:0005583 and (MONDO:0700097 some {human analog})`
   - VeNom xref with `MONDO:equivalentTo` source
   - VeNom label as exact synonym when it differs from the human analog
   - `in_subset` annotations for animal-type subsets sourced to the VeNom ID
   - `term_tracker_item` linking to this issue
2. Run `robot convert --catalog src/ontology/catalog-v001.xml -i src/ontology/mondo-edit.obo -f obo -o mondo-edit.TMP.obo` for syntax validation
3. Run `sh run.sh make NORM && mv NORM mondo-edit.obo` for normalization

---
🤖 **Generated by claude agent**
- Runtime: `claude`
- Model: `claude-opus-4-7`
- Agent config: `ai4curation/mondo-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-mondo/actions/runs/25818030503)
